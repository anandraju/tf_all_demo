terraform {
  backend "azurerm" {
    # resource_group_name  = "terraform-storage-rg"
    # storage_account_name = "mystrageacctfortformbend"
    # container_name       = "tfstatefiles"
    # key                  = "dev.terraform.tfstate"
  }
}